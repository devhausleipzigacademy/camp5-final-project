import React from "react";
const Header = () => {
  return (
    <div className="flex justify-center items-center w-full h-16 bg-primary text-primary-text font-semibold">
      <p>Dashboard</p>
    </div>
  );
};

export default Header;
